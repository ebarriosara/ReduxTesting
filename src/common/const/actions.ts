const prefix : string = 'LOGIN_MODULE';

export const actionDefs = {
  UPDATE_LOGIN_CREDENTIALS: `[${prefix}][1] Send the Credentials from the field values (component) to the state`,
  PERFORM_LOGIN_COMPLETED: `[${prefix}][2] Perform login completed`,
}